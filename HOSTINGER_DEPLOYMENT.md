# GateKaru - Hostinger Node.js & MySQL Deployment Guide 🚀
### (विशेष रूप से बिना कोडिंग ज्ञान वाले शुरुआती लोगों के लिए - Specially Written for Absolute Beginners)

नमस्ते! इस प्रोजेक्ट को इस तरह डिज़ाइन किया गया है कि आपको **कोडिंग (Coding), SQL, या डेटाबेस मैनेजमेंट** सीखने की बिल्कुल ज़रूरत नहीं है। हमारा स्मार्ट बैकएंड आपके लिए सब कुछ खुद-ब-खुद (Autopilot पर) संभाल लेगा!

यह गाइड आपको बहुत ही आसान भाषा में **Hostinger hPanel** पर आपका GateKaru ERP लाइव करने का तरीका बताएगी।

---

## 🌟 मुख्य विशेषताएं (Core Features of this Bundle):
* **✅ Auto MySQL Connector**: डेटाबेस क्रेडेंशियल्स सेट करते ही खुद-ब-खुद कनेक्ट हो जाता है।
* **✅ Auto Database Creator**: आपको phpMyAdmin में जाकर कोई SQL फ़ाइल इम्पोर्ट करने की ज़रूरत नहीं है। पहली बार ऐप चालू होने पर सभी ज़रूरी टेबल्स (users, visitors, notices, complaints, documents, staff) अपने आप बन जाएंगे।
* **✅ Auto Seeds (डिफ़ॉल्ट डेटा)**: टेबल्स बनते ही, सुपर एडमिन, गार्ड्स और सैंपल सोसाइटी डेटा अपने आप स्टोर हो जाएगा।
* **✅ One-Click Setup**: Hostinger के रेडीमेड बटन का उपयोग करके पूरा प्रोजेक्ट लाइव करें।

---

## 🛠️ Step-by-Step Deployment Instructions

### 📌 Step 1: Hostinger में MySQL Database बनाएँ (Create MySQL Database)
सबसे पहले, आपके डेटा को सुरक्षित रखने के लिए हमें एक खाली डेटाबेस की आवश्यकता होगी। इसे बनाने का तरीका:

1. अपने **Hostinger hPanel** में लॉग इन करें।
2. सर्च बार में **MySQL Databases** खोजें या बाएं मेनू से **Databases > MySQL Databases** पर जाएं।
3. **Create New MySQL Database and User** सेक्शन देखें:
   * **MySQL Database name**: कोई भी नाम लिखें (उदाहरण: `gate_db`). Hostinger इस नाम के आगे एक प्रीफ़िक्स जोड़ देगा (जैसे: `u931056402_gate_db`)।
   * **MySQL Username**: कोई भी यूजरनेम लिखें (उदाहरण: `gate_user`). (जैसे: `u931056402_gate_user`)।
   * **Password**: एक मजबूत पासवर्ड बनाएं (आप **Generate** बटन पर भी क्लिक कर सकते हैं)।
4. **इस पासवर्ड और पूरे डेटाबेस नेम / यूजरनेम को कहीं सुरक्षित नोट कर लें!**
5. **Create** बटन पर क्लिक करें।
6. अब नीचे बने हुए डेटाबेस की लिस्ट में जाएं। वहाँ आपको **MySQL Host** दिखेगा (आमतौर पर यह `localhost` या `127.0.0.1` होता है)। इसे भी नोट कर लें।

---

### 📂 Step 2: ZIP File अपलोड और एक्सट्रेक्ट करें (Upload & Extract ZIP)
अब हम आपके प्रोजेक्ट की फाइलों को Hostinger के सर्वर पर अपलोड करेंगे:

1. AI Studio में उत्पन्न हुई `gatekaru-hostinger-production.zip` फ़ाइल को अपने कंप्यूटर पर डाउनलोड करें।
2. Hostinger hPanel में जाएं और **File Manager** (फ़ाइल प्रबंधक) खोलें।
3. अपने डोमेन के मुख्य फ़ोल्डर (आमतौर पर यह **`public_html`** होता है, या फिर आपके द्वारा Node.js के लिए बनाया गया कोई सब-फ़ोल्डर) के अंदर जाएं।
4. ऊपर दाईं ओर दिख रहे **Upload File** (तीर का निशान) पर क्लिक करें और `gatekaru-hostinger-production.zip` फ़ाइल को अपलोड करें।
5. अपलोड पूरा होने के बाद, ज़िप फ़ाइल पर राइट-क्लिक करें और **Extract** चुनें:
   * एक्सट्रेक्ट करते समय फोल्डर पाथ में केवल `.` (डॉट) डालें या उसे खाली छोड़ दें ताकि फाइलें किसी नए सब-फोल्डर के बजाय सीधे उसी फोल्डर में एक्सट्रेक्ट हों।
   * सुनिश्चित करें कि `package.json`, `server.js`, और `src` आदि सीधे इसी डायरेक्टरी में दिखाई दे रहे हों।
6. एक्सट्रेक्ट होने के बाद, स्पेस बचाने के लिए मूल `.zip` फ़ाइल को **Delete** कर दें।

---

### ⚙️ Step 3: Node.js Application सेटअप करें (Setup Node.js App)
Hostinger में Node.js को एक्टिवेट करने के लिए यह सेटिंग्स करें:

1. Hostinger hPanel के सर्च बार में **Node.js** सर्च करें और उसे खोलें।
2. **Create Application** बटन पर क्लिक करें।
3. निम्नलिखित सेटिंग्स को ध्यान से भरें:
   * **Node.js Version**: `18.x` या `20.x` चुनें (नवीनतम स्थिर वर्शन)।
   * **Application Document Root**: आपका वही फोल्डर जहाँ फाइलें एक्सट्रेक्ट की हैं (उदा: `/public_html` या आपका डोमेन फोल्डर)।
   * **Application Entry Point**: **`server.js`** डालें (यह फ़ाइल आपके लिए पहले से बनी हुई है)।
   * **Passenger App Env**: `production` चुनें।
4. **Create / Save** पर क्लिक करें।

---

### 🔑 Step 4: Environment Variables (पर्यावरण चर) सेट करें
यह सबसे महत्वपूर्ण कदम है ताकि आपका ऐप डेटाबेस से बात कर सके:

1. Node.js Dashboard में थोड़ा नीचे स्क्रॉल करें, आपको **Environment Variables** (पर्यावरण चर) का सेक्शन मिलेगा।
2. **Add Variable** पर क्लिक करें और एक-एक करके निम्नलिखित 5 वेरिएबल्स को जोड़ें (उनके नाम बिल्कुल सही और कैपिटल लेटर्स में होने चाहिए):

| Variable Name (नाम) | Value (वैल्यू) | उदाहरण (Example) |
| :--- | :--- | :--- |
| **`DB_HOST`** | आपका MySQL Host | `localhost` (या Hostinger द्वारा दिया गया होस्ट) |
| **`DB_PORT`** | MySQL का पोर्ट नंबर | `3306` |
| **`DB_USER`** | आपका MySQL यूजरनेम जो Step 1 में बनाया था | `u931056402_gate_user` |
| **`DB_NAME`** | आपका MySQL डेटाबेस नाम जो Step 1 में बनाया था | `u931056402_gate_db` |
| **`DB_PASSWORD`** | डेटाबेस का पासवर्ड जो Step 1 में बनाया था | `MySecurePassword123!` |
| **`JWT_SECRET`** | कोई भी सुरक्षित रैंडम टेक्स्ट (लॉगिन टोकन के लिए) | `gatekaru_secure_jwt_secret_key_99` |
| **`NODE_ENV`** | एप्लिकेशन मोड | `production` |

3. सभी वेरिएबल्स को सेव (Save) कर दें।

---

### ⚡ Step 5: NPM Install और NPM Build रन करें (Install & Build)
आपके ऐप को शुरू करने के लिए हमें जरूरी पैकेजेस इंस्टॉल करने होंगे और कोड को कम्पाइल करना होगा:

1. Node.js hPanel Dashboard में जाएं।
2. वहाँ आपको **`NPM Install`** बटन दिखाई देगा। उस पर क्लिक करें।
   * यह आपके `package.json` में लिखी सभी डिपेंडेंसीज (जैसे `express`, `mysql2`, `react` आदि) को ऑटोमैटिक इंस्टॉल कर देगा। इसमें 1-2 मिनट लग सकते हैं।
3. इसके बाद, **`Run NPM Script`** विकल्प में जाएं:
   * स्क्रिप्ट ड्रॉपडाउन में **`build`** चुनें या बॉक्स में `build` टाइप करें और **Run** बटन पर क्लिक करें।
   * **यह कमांड सबसे महत्वपूर्ण है!** यह आपके React फ्रंटएंड को कम्पाइल करके `dist/` फ़ोल्डर बनाएगी और आपके TypeScript सर्वर को तेज़ `dist/server.cjs` में कन्वर्ट करेगी।
   * रन पूरा होने का इंतजार करें।

---

### 🚀 Step 6: Node.js एप्लिकेशन शुरू करें (Start Application)
1. अब सबसे ऊपर जाएं और **Start Application** या **Start/Restart** बटन पर क्लिक करें।
2. **बधाई हो! 🎉** आपका एप्लीकेशन अब पूरी तरह चालू है।
3. अपने डोमेन पर जाएं (जैसे `https://yourdomain.com`)। आपका GateKaru ऐप लोड हो जाएगा और डेटाबेस में सभी टेबल्स और शुरुआती डेटा खुद-ब-खुद बन जाएगा!

---

## 🔑 Default Login Credentials (शुरुआती लॉगिन क्रेडेंशियल्स)
लॉग इन करने के लिए निम्नलिखित प्रोफाइल्स का उपयोग करें (सभी के लिए OTP `123456` है):

1. **Super Admin (पूरे सिस्टम का मालिक - SaaS Admin)**:
   * **Phone**: `+91 99999 99999`
   * **OTP**: `123456`

2. **Society Committee Admin (सोसाइटी एडमिन)**:
   * **Phone**: `+91 98765 43210`
   * **OTP**: `123456`

3. **Gate Security Guard (सुरक्षा गार्ड)**:
   * **Phone**: `+91 91111 22222`
   * **OTP**: `123456`

4. **Resident (सोसाइटी का निवासी)**:
   * **Phone**: `+91 98112 34567`
   * **OTP**: `123456`

---

## 📱 Android App (Google Play Store के लिए तैयार)
इस ZIP फ़ाइल में एक रेडीमेड **`android/`** फ़ोल्डर भी शामिल है जो Capacitor से कंफिगर है:
1. अपने कंप्यूटर पर ज़िप फ़ाइल खोलें और `android` फ़ोल्डर को देखें।
2. **Android Studio** खोलें और **Open Project** पर क्लिक करके `android` फ़ोल्डर चुनें।
3. Android Studio अपने आप ग्रैडल डाउनलोड और सिंक कर देगा।
4. Play Store या टेस्टिंग के लिए APK/AAB बनाने के लिए ऊपर मेनू में **Build > Generate Signed Bundle / APK...** पर क्लिक करें और निर्देशों का पालन करें।

---

## ❓ अक्सर पूछे जाने वाले सवाल और समाधान (Troubleshooting Guide)

#### Q1. स्क्रीन पर "503 Service Unavailable" या "Please wait..." आ रहा है?
* **समाधान**: इसका मतलब है कि ऐप अभी स्टार्ट नहीं हुआ है। Hostinger Node.js Dashboard में जाकर **Restart Application** पर क्लिक करें। यह भी सुनिश्चित करें कि `NPM Install` और `NPM Build` सफलतापूर्वक पूरा हुआ था।

#### Q2. "Database Connection Failed" एरर आ रही है?
* **समाधान**: कृपया दोबारा जांचें कि आपने Step 4 में जो Environment Variables डाले हैं, वे बिल्कुल सही हैं या नहीं। खासकर `DB_NAME`, `DB_USER`, और `DB_PASSWORD` में कोई स्पेलिंग मिस्टेक तो नहीं है। कुछ मामलों में Hostinger में `DB_HOST` की वैल्यू `localhost` के बजाय `127.0.0.1` या डेटाबेस पेज पर दी गई कोई विशिष्ट IP हो सकती है।

#### Q3. क्या मुझे phpMyAdmin में जाकर कोई SQL टेबल्स बनानी होंगी?
* **समाधान**: **बिल्कुल नहीं!** हमने कोड में `Auto-Migration` और `Auto-Seed` मैकेनिज्म लिखा है। जैसे ही ऐप पहली बार लाइव होगा, वह आपके लिए पूरी डेटाबेस स्कीमा और डिफ़ॉल्ट डेटा खुद बना देगा।
